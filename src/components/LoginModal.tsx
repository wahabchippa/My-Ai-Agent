"use client";
// Legacy LoginModal — not used in current UI (replaced by AuthScreen)
// Kept for backward compatibility with any code that references it.

export function LoginModal({ onLogin }: { onLogin: (user: any) => void }) {
  return null;
}
